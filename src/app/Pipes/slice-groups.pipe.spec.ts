import { SliceGroupsPipe } from './slice-groups.pipe';

describe('SliceGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
